import { getEvaluacion, tieneTexto } from "./evaluationViewData";

export default function BarreraAplicacionCard() {
  const data = getEvaluacion().seguimientoAplicacionCml;
  if (!data || !Object.values(data).some(value=>Array.isArray(value)?value.length:tieneTexto(value))) return null;
  return <article className="overflow-hidden rounded-xl border border-[#e1e7ed] bg-white shadow-[0_8px_24px_rgba(35,52,70,0.04)]"><div className="grid md:grid-cols-[0.9fr_1.1fr]"><div className="border-b border-[#edf1f4] p-5 md:border-b-0 md:border-r md:p-6"><span className="inline-flex rounded-full bg-[#eef5fc] px-3 py-1 text-[10px] font-semibold uppercase tracking-[0.08em] text-[#46729a]">{data.etiqueta}</span><p className="mt-5 text-[15px] font-semibold leading-relaxed text-[#263d52] sm:text-base">{data.programados}</p><ul className="mt-5 space-y-3 border-t border-[#edf1f4] pt-4 text-[14px] leading-relaxed text-[#5d7184] sm:text-[15px]">{data.aplicaciones.map(item=><li key={item} className="flex gap-3"><span className="mt-[9px] h-1.5 w-1.5 shrink-0 rounded-full bg-[#6f8ba3]"/><span>{item}</span></li>)}</ul></div><div className="flex gap-4 bg-[#fffafa] p-5 md:p-6"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full border border-[#f2cccc] bg-white text-lg font-bold text-[#c91c1c]">!</span><p className="text-[14px] leading-relaxed text-[#4a4f55] sm:text-[15px]"><span className="font-semibold text-[#a62626]">Barrera:</span>{" "}{data.barrera}</p></div></div></article>;
}
