import type { Finding } from "./qualityData";

export default function FindingsTable({ data }: { data: Finding[] }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[600px] border-collapse"><thead><tr className="border-y border-[#dfe6ed] bg-[#f8fafc] text-left">{["Hallazgo / indicador", "Componente", "Magnitud", "%", "Nivel"].map((heading) => <th key={heading} className="px-3 py-3 text-[7px] font-semibold uppercase text-[#8497ab] first:pl-4">{heading}</th>)}</tr></thead><tbody>{data.map((item) => <tr key={item.title} className="border-b border-[#e4eaf0] align-top"><td className="px-4 py-3"><p className="text-[8px] font-medium text-[#183049]">{item.title}</p><p className="mt-1 max-w-[270px] text-[7px] leading-[1.45] text-[#8a9bad]">{item.description}</p></td><td className="px-3 py-3 text-[7px] leading-5 text-[#6f8294]">{item.component}</td><td className="px-3 py-3 text-[8px] font-semibold leading-5 text-[#1f3447]">{item.magnitude}</td><td className="px-3 py-3 text-[8px] font-semibold text-[#1f3447]">{item.percentage}</td><td className="px-3 py-3"><span className="rounded-full bg-[#ffe8e8] px-2 py-1 text-[7px] font-semibold text-[#f04444]">{item.level}</span></td></tr>)}</tbody></table></div>;
}
